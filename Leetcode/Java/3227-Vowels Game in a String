class Solution {
    public boolean doesAliceWin(String s) {
        return IntStream.range(0, s.length())
                .map(i -> Set.of('a', 'e', 'i', 'o', 'u').contains(s.charAt(i)) ? 1 : 0)
                .sum() != 0;
    }
}
