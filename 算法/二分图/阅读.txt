/*
二分图：

二分图 => 不存在奇树环 => 染色无矛盾 

增广路：
非匹配点 -> 非匹配边 -> 匹配边 -> 非匹配边 ... -> 非匹配点
最大匹配 = 不存在增广路


最大匹配数 = 最小点覆盖 = 点总数 - 最大独立集合 = 总点数 - 最小路劲点覆盖
最小点覆盖 ：最少的点覆盖所有的边



最小路劲点覆盖: DAG(有向无环图), 转化后成无环图
用最少条互不相交的路劲(点不重复)讲所有点覆盖住。
过程:
对每个点拆点(u, u')。
u -> v 的边, 新图 u -> v'
1. 原图中的路劲 <=> 新图一个匹配
2. 原图每个路劲终点没有出边 <=> 对应左边的非匹配点
求最小路劲点覆盖 = 求左边最少的非匹配点数量 => 左侧匹配点最多


最小路劲重复点覆盖: 两条路劲可以有公共点
1. 求传递闭包(新图)
2. 原图最小路劲重复点覆盖 = 新图最小路劲点覆盖
*/
